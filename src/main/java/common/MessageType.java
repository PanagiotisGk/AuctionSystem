package common;

public enum MessageType {
    REGISTER,
    REGISTER_RESPONSE,
    LOGIN,
    LOGIN_RESPONSE,
    LOGOUT,
    LOGOUT_RESPONSE,
    REQUEST_AUCTION,
    REQUEST_AUCTION_RESPONSE,
    GET_CURRENT_AUCTION,
    GET_CURRENT_AUCTION_RESPONSE,
    ERROR
}