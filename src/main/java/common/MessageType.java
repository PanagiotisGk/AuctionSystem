package common;

public enum MessageType {
    REGISTER,
    REGISTER_RESPONSE,
    LOGIN,
    LOGIN_RESPONSE,
    LOGOUT,
    LOGOUT_RESPONSE,
    REQUEST_AUCTION,
    REQUEST_AUCTION_RESPONSE,
    ERROR
}
